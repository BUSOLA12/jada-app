{
  "name": "jada-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "rider": "npm --workspace apps/rider run start",
    "driver": "npm --workspace apps/driver run start",
    "install:all": "npm install"
  }
}
